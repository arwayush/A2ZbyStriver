package Step1.Collections;

import java.util.ArrayDeque;
import java.util.Queue;

public class Stack {
    public static void main(String[] args) {

    }
}


/*
Elements in the stack are accessed in the manner of LIFO
Methods related to the stack are
1. push()
2. pop()
3. peek()
4. empty()
5. isEmpty() -> taken from the collection framework

add(E,e) -> in the queue, it will insert the specific element into the queue
add(E,e) ->
remove() -> it will remove the element from the fromt of queue, will throw an exception
poll() -> removes and returns the element at the front of the queue, will throw null exception
element() -> retrieves the element from the front, throws the exception if the queue is empty
peek() -> peeks the elment and returns the null iif queue is empty



 */